# Variáveis de ambiente do server. Copie para `.env.local` e ajuste quando necessário.
# `.env.local` é ignorado pelo git; este arquivo (.env.example) é o template versionado.

# Porta HTTP do server (opcional; default 3000).
PORT=3000
